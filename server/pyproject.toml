[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sequre-server"
version = "0.1.0"
description = "seQure relay server"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["seQure_server"]
